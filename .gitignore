# Python
__pycache__/
*.py[cod]
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Node
node_modules/
.next/
.turbo/

# Env
.env
.env.local
.env.*.local

# OS
.DS_Store

# Lambda build artefacts (generated by `make api-reqs` / `sam build`)
apps/api/requirements.txt
apps/api/vendor/
.aws-sam/

# Next.js incremental typecheck state
*.tsbuildinfo

# Copied from vocab/shapes/ by `make api-reqs`; vocab/ is the original.
packages/h2o_core/src/h2o_core/shapes/

# Claude Code session state: an accumulated permission allowlist, not shared
# configuration. It fills with one-off entries (a specific sed invocation, a cp
# from ~/Downloads) that mean nothing to anyone else.
.claude/
